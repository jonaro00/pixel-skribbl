syntax = "proto3";
package chat;

service Chat {
    rpc SendMessage (ChatMessage) returns (ChatResponse);
}
message ChatMessage {
    string user = 1;
    string message = 2;
}
message ChatResponse {
    bool success = 1;
}
